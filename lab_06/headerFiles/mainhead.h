#ifndef MAINHEAD_H
#define MAINHEAD_H

#include <stdio.h>
#include <malloc.h>
#include <stdlib.h>

#define CMP_EQ(a, b) ((a) == (b))
#define CMP_LT(a, b) ((a) < (b))
#define CMP_GT(a, b) ((a) > (b))

#endif